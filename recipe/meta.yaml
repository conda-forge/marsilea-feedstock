{% set name = "marsilea" %}
{% set version = "0.8.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://files.pythonhosted.org/packages/source/{{ name[0] }}/{{ name }}/marsilea-{{ version }}.tar.gz
  sha256: a713b665b5f5a4972bcee838bd127ff0707ca19c73595c54f20c81b30b28e90c

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - hatchling
    - pip
  run:
    - python >={{ python_min }}
    - numpy
    - pandas
    # pandas[parquet] extra, needed by marsilea.dataset for read_parquet
    - pyarrow
    - matplotlib-base >=3.6
    - seaborn
    - scipy
    - legendkit >=0.4.1
    - platformdirs

test:
  imports:
    - marsilea
    - oncoprinter
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/Marsilea-viz/marsilea
  summary: Declarative creation of composable visualization for Python
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - Mr-Milk
